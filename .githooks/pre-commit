#!/usr/bin/env sh

printf '%s\n' '[pre-commit] Running text quality guardrail...'
npm run check:text
status=$?

if [ "$status" -ne 0 ]; then
  printf '%s\n' '[pre-commit] Blocked commit: text quality check failed.'
  exit "$status"
fi

printf '%s\n' '[pre-commit] OK.'
exit 0
